# Terraform working files — never commit state (may contain secrets) or plugins.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
tfplan
*.tfplan
crash.log
# Keep .terraform.lock.hcl committed for reproducible provider versions.
